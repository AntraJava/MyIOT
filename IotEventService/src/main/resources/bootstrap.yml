spring:
  application:
    name : IotEventService
  profiles:
    active:
      dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: 12345678
      username: config-server-user
